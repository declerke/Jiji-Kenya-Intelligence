FROM node:20-slim

WORKDIR /app

# Copy package manifest and install all dependencies at build time
# This avoids runtime npm install and network flakiness
COPY reports/package.json ./package.json

RUN npm install --legacy-peer-deps --ignore-scripts && npm rebuild duckdb

# Copy Evidence project files
COPY reports/ ./
